<form method="post" action="{{ path('app_calidad_delete', {'id': calidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calidad.id) }}">
    <button class="btn">Delete</button>
</form>
