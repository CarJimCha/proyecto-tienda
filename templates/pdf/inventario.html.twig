<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Inventario de {{ usuarioSeleccionado.characterName }}</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 12px; }
        h1 { text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td {
            border: 1px solid #000;
            padding: 6px;
            text-align: center;
        }
    </style>

</head>
<body>
<h1>Inventario de {{ usuarioSeleccionado.characterName }}</h1>

<table>
    <thead>
    <tr>
        <th>Cantidad</th>
        <th>Item</th>
        <th>Categoría</th>
        <th>Calidad</th>
        <th>Precio medio de compra</th>
    </tr>
    </thead>
    <tbody>
    {% for row in inventario %}
        <tr>
            <td>{{ row.totalCantidad }}</td>
            <td>{{ row.itemNombre }}</td>
            <td>{{ row.categoriaNombre }}</td>
            <td>{{ row.calidadNombre }}</td>
            <td>{{ row.precioMedio|moneda_formateada }}</td>
        </tr>
    {% else %}
        <tr><td colspan="5">Inventario vacío.</td></tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
