{% extends 'base.html.twig' %}

{% block title %}{{ item.nombre }}{% endblock %}

{% block body %}
    <h1>Mostrar Artículo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ item.nombre }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ item.precio }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ item.categoria }}</td>
            </tr>
            <tr>
                <th>Peso</th>
                <td>{{ item.peso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_item_index') }}" class="btn">Volver a la lista</a>

    <br><a href="{{ path('app_item_edit', {'id': item.id}) }}" class="btn">Editar</a>

    {{ include('item/_delete_form.html.twig') }}
{% endblock %}
